# 本地部署与真实页面协议验收

日期：2026-09-09 至 2026-09-10。本文记录真实 Manager 页面、真实 Node-RED 与本地真实 ThingLinks Cloud 的操作结果；设备由独立协议模拟器提供。本轮七种采集模板、六种可用控制模板、最新影子和实例启停状态同步已完成下述验收，文末范围外能力不视为通过。

历史阶段记录：当前结论请查看[后续正式节点协议验收](protocol-wire-nodered-20260910.md)。尤其不能用本页的Modbus正向控制结果覆盖后续断网迟到写失败，也不能把以下旧镜像标识当作当前部署。

## 部署边界

- 当前部署：`thinglinks-edge-local-ui:presence-final-20260910`，镜像 `sha256:5c252aed6fc8a584ee37ee09adeaa61aadc31d66105937db9f702726a20128f5`，Manager 容器 `11272ab8be77fabe79aecd007270f341341d72fc110f7a1cbb094ac61c3d4d9e`，数据库 schema 17。端口 19100 健康检查通过。
- 当前代码的 `pnpm check`、`pnpm build`、Docker 构建均通过：Manager 1315 通过、3 个既有跳过，Web 69 通过；状态同步 56 项定向测试通过并经独立复审。含真实 MQTT 客户端断线重放测试、真实 Dockerode HTTP socket 取消测试。
- 以下为分阶段部署记录；首次协议修复将数据库升级到 schema 16。
- 首轮镜像 `thinglinks-edge-local-ui:run-1788955197359`，镜像摘要 `sha256:a2387d88106ef20e2017d1c65fa30b4d6b6904a9dc20c14970533eee81c6d710`。
- 更新前使用既有备份机制完成加密备份，并保留旧镜像标签。没有复制或记录登录口令、主密钥和实例令牌。
- `line-1` 容器、镜像、启动时间、重启次数及设置/包清单摘要保持不变。
- 通过实际页面创建临时实例 `ui-protocol-0909`。所有模板部署、节点批准和安装均从页面操作；未通过接口直接写入流程。
- 设备模拟器只提供 TCP、UDP、HTTP、Modbus、OPC UA、S7 的设备端行为，不替代 Manager 的接口、数据库、缓存或命令执行逻辑。
- 第二轮修复镜像已部署：`thinglinks-edge-local-ui:fix-1788955197359`，摘要 `sha256:8f299d4c5ee2006015f849403bf70bdd305e4e8415eacf39f0dd33d38fa5244f`；Manager 容器 `4e47c7095b67b1f3dfee9e6f9ba85d7494967fd925539b34d2a165b58114dc2b`。
- 该批完整 `pnpm check` 与 `pnpm build` 通过：Manager 1278 通过、3 个既有跳过；Web 59 通过。当时旧模板告警读时重算修复尚未进入镜像，随后已纳入下述最终镜像。
- 上一轮修复镜像：`thinglinks-edge-local-ui:final-1788955197359`，摘要 `sha256:02adfdeefe07bd646ce229414b8da79aa84fb5f38345cf07f5cb8f94799cf295`，容器 `ffa64161788c74e4f4c18df6a1f6ceea0e79ae1cb2fac88c9110e031a77658c6`。已包含旧模板读时重扫告警、停止实例状态展示与刷新修复。
- 该阶段 `pnpm check`、`pnpm build` 和镜像构建通过：Manager 1282 通过、3 个既有跳过；Web 69 通过。更新后原 `line-1` 与验收 Node-RED 的 ID、启动时间均未变化。

## 已完成的页面检查

| 场景 | 页面操作与实际证据 |
|---|---|
| 模板分类和搜索 | 七份内置模板可见；工业分类显示 Modbus、OPC UA、S7；搜索收敛到对应模板 |
| TCP | 配置客户端连接、两点映射，追加部署；设备值 `25/60 → 31.5/64`，现场页自动更新；趋势出现两组样本 |
| TCP 异常报文 | 设备连续发送无效 JSON 时，页面保留上次有效值 31.5，更新时间正常变旧；恢复合法报文后自动显示新值 39 |
| UDP | 配置接收端口，倍率 2、偏移 1；设备原值 `25 → 28`，现场页 `51 → 57` |
| HTTP 定时读取 | 配置 2 秒周期，部署真实请求节点；设备值 `25 → 33`，现场页自动更新 |
| HTTP 异常恢复 | 连续 20 秒返回错误响应时保留有效值 33；故障解除后自动更新到 38，没有因请求占用标记遗留而停止采集 |
| HTTP 主动推送 | 真实设备请求收到 HTTP 202，现场页显示 33；非法数值收到 HTTP 400，页面保留上次有效值 |
| Modbus-TCP | 未安装时阻止部署；通过批准清单批准 5.60.2，再从模板页面安装到临时实例；组件显示已加载，部署后寄存器 250 映射为 25；294 映射后页面暴露浮点尾数展示问题，待修复复验 |
| OPC UA 采集 | 用户回复继续后批准 0.2.355，经页面安装和部署；实际变量 `25 → 36.5` 在现场页更新。开启控制时发现 `exec` 被默认安全配置禁止，控制没有部署 |
| S7 | 页面批准、安装 3.1.3，配置 `DB1,REAL0` 并部署；实际 PLC 模拟内存值 `25 → 34.5` 在现场页更新 |
| 追加保护 | TCP、UDP、HTTP、Modbus 依次追加，此前流程继续采集 |
| 监听冲突 | 再次使用 UDP 15002 端口，预览列出占用节点并禁用确认部署 |
| 未配置云连接 | 读取物模型明确失败并保留已填参数；云平台页显示未配置、上行 0，测试数据进入待补传 |

## 页面发现与修复

1. 配置副本参数档案已修复；真实 TCP 副本的恢复、另存、重新打开及按编辑参数部署均已从页面通过。显式快照切换入口也已从页面通过。
2. 固定凭据误报已修复，真实 TCP/HTTP 等新副本及预览无误报；2026-09-10 最终镜像页面复验中，旧卡片“UI验收-TCP配置副本”已不再显示误报告警，仍按旧静态副本提供“套用到实例”。
3. 部署忙碌状态和对话框代次隔离已修复并测试；OPC UA、S7 副本关闭后重新打开均恢复各自参数。前序锁屏阻塞已解除，后续已在物模型查询仍显示 loading 时关闭并切换模板，确认新窗口未被旧结果覆盖，具体证据见后文。
4. Modbus 浮点尾数已在显示层修复。2026-09-10 页面显示“≈ 29.4”，点击后显示 Manager 原始值 `29.400000000000002`，通过复验。
5. 2026-09-10 启动临时实例前，现场页显示 12 台设备、0 台在线；对应设备均显示“实例已停止”，展开后明确提示保留最后一次采集值，列名为“最后采集值”。实例启动并完成新协议部署后在线数为 7，旧停止实例仍未计入。

## 真实 ThingLinks 联调进展

用户随后确认本地 ThingLinks 与 MQTT 已运行并允许复用已有产品设备。通过已登录的本地控制台 `http://localhost:3100` 操作，复用网关 `2130020836696065`（智慧大棚1号温室LoRa网关），客户端 `2130020836696064@1`。Edge 通过 `mqtt://host.docker.internal:11883` 接入；凭据仅通过页面复制与粘贴传递，未写入报告或测试文件。Edge 显示已连接，ThingLinks 查询确认该网关在线。

- 先从页面停止旧临时实例。停止 Manager 后逐条校验离线段文件的完整性和测试设备归属，完整归档 9097 条本轮模拟记录到 `manager/spool-ui-validation-1788955197359`，没有删除记录；新缓存为空，避免把未登记的旧测试标识上报给真实 Cloud。
- 通过页面创建新的 `ui-cloud-0909` 实例，保留原实例和 `line-1`。
- 两个既有产品都是网关模型，不能真实表达本轮温度点位写入。通过 Cloud 快捷生成页面创建临时普通产品 `8708895600832512`，名称“Edge协议验收-温度设备（临时0909）”。首版因服务默认停用没有可用服务；在服务编辑页启用后重新发布，当前版本为 `8709376284848128`。只有 `telemetry.temperature`（decimal、℃、-40..120）和 `set_temperature(value)`（decimal、℃、0..100）参与验收。
- 通过 Cloud 设备复制页面创建首台网关子设备 `8717106890436609`，名称“Edge协议验收-TCP-0909”，绑定上述产品版本及既有网关。
- 在 Edge 模板页面发起真实 MQTT `model/query`，读取该明确产品/版本成功。页面显示服务名称与编码；输入 `not_in_model` 时禁止预览、部署及保存，修正后恢复。
- 新 TCP 配置副本重新打开后完整恢复设备、版本、点位和控制参数；显式切换保存快照会隐藏参数编辑并显示保存时设备，切回保留编辑。修改倍率为 2 后另存为“真实云端验收-TCP倍率2-0909”，再次打开仍为 2，无旧的固定凭据误报。
- 由该副本部署到 `ui-cloud-0909`，Edge 实际温度为 50（设备原值 25，倍率 2）。最初 Cloud 最新影子为空，但后续独立时序查询和 Cloud 影子详情的历史表/趋势图证明数据已落库。该异常已定位为最新查询列名未归一，不能归因于 MQTT 上报失败。

### 真实云端已通过的链路

所有配置、保存副本、安装、预览、部署和命令下发均由真实页面完成；设备模拟器仅负责物理协议端的值变化和报文发送。下表的回执由真实 Cloud 网关命令历史确认，不以“发送成功”代替设备执行。

| 场景 | 子设备标识 | 已验证结果 |
|---|---|---|
| TCP | `8717106890436609` | 原值 25 经倍率 2 显示/上报 50；Cloud 下发 54 后原值写为 27，写入 1 次，标准回执 errCode=0；实际时序与 Cloud 历史页面连续显示 54 |
| UDP | `8726321222676481` | 实际报文 25 入库；Cloud 下发 37，写入 1 次并回执成功；再次报文上行 37 入库 |
| HTTP 定时 | `8730642605699073` | 2 秒采集，25 入库；Cloud 下发 32，HTTP 设备确认成功，后续定时上报更新；与推送测试共用一个 HTTP 模拟设备端 |
| HTTP 推送 | `8736214654676993` | POST 返回 202、32 入库；Cloud 下发 41，HTTP 写入次数从 1 增至 2并成功回执；随后推送 41 入库 |
| Modbus-TCP | `8738959755341825` | 从页面安装 5.60.2并部署；寄存器 250 按0.1倍率为25且入库；下发35写入寄存器350、写入1次，成功回执并上报35 |
| OPC UA | `8739070908592129` | 2026-09-10 页面安装 0.2.355，配置匿名端点 `opc.tcp://protocol-simulator:15040/UA/Fixture`、`ns=1;s=Temperature`、2 秒读取；保存副本后重开参数完整，读取真实产品/版本模型，追加 11 个节点；实际变量 25→36.5，Edge 页面和真实时序库同步更新。控制开关点击后仍关闭，页面明确说明待独立受控执行节点交付 |
| S7 | `8739140626313217` | 2026-09-10 页面安装 3.1.3，配置 `protocol-simulator:15030`、`DB1,REAL0`、2 秒读取，保存并追加 22 个节点；25 采集入库；Cloud 页面下发 43，模拟 PLC 执行 1 次 WriteVar，内存 REAL 为 43，Edge 与真实时序均为 43，网关标准回执 errCode=0 |

TCP 请求/回执 MID `8722772082323456`、UDP 回执 MID `8729012028076032`；原请求和回执关联证据保留在本轮查询记录中。Edge 控制记录页面也明确显示 TCP 的设备执行成功、模型校验已执行、回执已发布。

最终 Manager 更新后，五条链路又分别查询到真实时序 `54 / 37 / 41 / 41 / 35`。模拟设备写入次数前后均为 TCP 1、UDP 1、HTTP 2、Modbus 1、S7 0、OPC UA 0；没有因 Manager 更新产生额外设备写入。

2026-09-10 08:23:03，S7 成功命令与回执使用同一 MID `8867032467271680`。Edge 副本“近期控制记录”明确显示设备执行成功、已执行模型校验、回执已发布；Cloud 网关命令响应体指向 S7 子设备，返回 `value=43`。

08:27 补测 S7 断线：模拟端断线 20 秒期间从 Cloud 页面下发 44，网关收到 MID `8868118603264000` 的 `errCode=1` 回执，说明“设备执行结果未知：Error: Not connected”。模拟 PLC 仍为 43、写入次数仍为 1。断线解除后，08:28:55 至 08:28:59 的真实时序继续为 43，写入次数未增加，未自动重放失败写操作。

08:30 同时复核七条真实链路，时序温度依次为 TCP 54、UDP 37、HTTP 定时 41、HTTP 推送 41、Modbus 35、OPC UA 36.5、S7 43 ℃。UDP 和 HTTP 推送在本轮重新触发设备报文，HTTP 返回 202。追加 OPC UA、S7 后此前五条流程仍正常上云。

本轮脱敏证据保存在 `/tmp/tle-live-ui-1788955197359/real-cloud-validation-20260910.json`，包含真实时序样本、S7 成功/失败回执摘要及模拟设备写入计数；不包含登录或接入凭据。

### Cloud 最新影子修复

在隔离 TDengine 3.3.6.13 与当前应用使用的 TAOS JDBC 3.8.3 中复现：`last(*)` 的列标签为 `last(ts)`、`last(temperature)` 等，Cloud 影子却按原字段名读取。交错空值测试证明必须保留每列最新非空值，不能替换成最后整行。库表名参数绑定在真实驱动中可用，因此没有修改 SQL。

仅修改 Cloud `TDengineManager.queryLatestPerColumn` 的列名归一及新增回归测试，24 项定向测试、修改前红测、独立窄审通过；独立诊断容器和网络已清理。用户随后重跑 TDS，2026-09-10 09:39:06 启动完成，18788 监听进程由 98369 换为 38259。

重启后七台子设备最新影子接口均返回正确数值；Cloud 设备管理页面逐台打开影子，确认 TCP 54、UDP 37、HTTP 定时 41、HTTP 推送 41、Modbus 35、OPC UA 36.50、S7 43 ℃，各自设备标签选中。随后从 Edge 页面启动临时实例，将 OPC UA 模拟值改为 37.5，Edge 显示 37.5，Cloud 页面自动刷新为 37.50（09:48:47），最新影子接口和 09:49:39–09:49:43 原始时序均为 37.5。TDS 最新值修复已通过真实运行时与页面复验。

同轮执行了查询中关闭/切换复验：S7 的物模型查询按钮仍显示 loading 时关闭，立即打开 OPC UA 副本，仍是 `8739070908592129`、`ns=1;s=Temperature`、控制关闭和云端映射未校验；后续再次检查结果未被上一轮查询覆盖。

### 子设备状态同步缺口与修复验收

数据与影子正确时，Cloud 子设备详情仍显示离线。实读当前源码确认，`datas` 不更新连接状态；父网关离线会把子设备置离线，恢复连接不会把子设备恢复在线。Edge 仅本地更新台账，已有 `CloudGateway.updateSubDeviceStatus()` 无运行时调用。这是状态同步缺口，不能解释为子设备没有直接 MQTT 会话。

已实现独立 `core/cloud/presence/` 状态同步层：通过官方 `topo/query` 查询实时上行候选并确认当前网关归属，再调用 `topo/update`，逐条核对回执；不扫描重推旧台账，不让历史补传恢复在线。明确停止/离线事件同步 OFFLINE，不暗设事件型设备静默超时。

定向与独立审查覆盖了晚到回执、ONLINE/OFFLINE 往返补偿、多来源、Manager 重启、未确认回执的持久化责任、来源容量上限和取消。真实 MQTT 客户端复现了旧 QoS1 ONLINE 在超时重连后仍重投的问题；状态请求现使用无离线队列的 QoS0 加业务回执确认，保持数据和命令原有 QoS。真实 Dockerode/modem 请求取消时，测试服务端 socket 确实关闭，不是仅放弃等待。

更新前通过既有备份库生成配置和数据库加密备份 `manager/backups/before-presence-sync-20260910.tle-backup`：46,026,788 字节，schema16，3 个实例，SHA-256 `2a62abf5fd21e3667faa8b0b76da11304480dce7a4469add42263bc2e7ca94c4`。此备份排除 `node_modules`，所有实例目录和已装组件原地保留；此前完整备份和旧镜像也保留。

真实运行验收：

1. 10:36 从 Edge 页面启动 `ui-cloud-0909`，触发 UDP、HTTP 推送报文。七台子设备均由 Cloud 确认为 ONLINE，并获得新的心跳时间。
2. 从 Edge 页面停止实例，七台均确认 OFFLINE；SQLite 中七来源为 OFFLINE、无来源冲突。用户指定改用侧边内置浏览器后，Cloud 卡片也逐台显示离线。
3. 从内置 Edge 页面重新启动：OPC UA 等定时设备凭新采样恢复 ONLINE；UDP 和 HTTP 推送在未收到新报文时仍为 OFFLINE。补发真实设备报文后，内置 Cloud 页面七台均显示在线。
4. 从内置 Cloud 的 MQTT 调试表单选择本轮产品、版本、S7 子设备及结构化命令，下发 46 ℃。S7 WriteVar 次数由 1 增至 2，实际温度 46，标准成功回执 MID `8904436741861376`、`errCode=0`；最新影子及原始时序均为 46，内置设备影子页面显示在线和 46 ℃。
5. 仅重启 Manager，原 `line-1` 和验收 Node-RED 的启动时间不变。五台定时/连接采集设备以新数据恢复 ONLINE；UDP、HTTP 推送仍为 OFFLINE，补发新报文后内置页面七台再次在线。模拟设备写入次数未增加，未执行旧命令。
6. 最后从内置 Edge 页面停止临时实例，10:56:37–10:56:39 七台在 Cloud 确认 OFFLINE，内置 Cloud 卡片逐台显示离线。测试数据、流程、组件、产品、设备及记录全部保留。

最终镜像与 Manager 重启后，七条链路最新原始时序依次为 54、37、41、41、35、37.5、46 ℃，时间为 10:54:37 至 10:56:31。脱敏运行证据见 `/tmp/tle-live-ui-1788955197359/presence-live-validation-20260910.json`；完整门槛日志为同目录 `presence-check-capacity-final.log`、`presence-build-capacity-final.log`、`presence-image-capacity-final.log`。

另有 Cloud 服务端归属校验边界：当前状态更新实现未使用网关字段过滤目标设备。上述 Edge 客户端校验用于收敛本轮更新范围，不代表 Cloud 服务端的归属校验已经修复。

### 续验过程与最终资源状态

前序曾因 Mac 锁屏暂停。用户重跑 TDS 后完成最新影子和模板切换复验；Cloud Chrome 登录过期后，用户明确指定使用侧边内置浏览器，后续状态及新 S7 命令验收均在内置浏览器完成。

最终 Manager 健康运行；`line-1` 容器 ID、镜像、启动时间和重启次数未变，`settings.js` 与 `package.json` SHA-256 仍分别为 `4a7344719be91c19b5fdc3f4e45a326b48a7455ee7a09bb32a9daed94c84b5c5`、`6b2442dfc3be5180f7d20f7f704a7c6d04a2bef4e64e9670a2c02bb13c33b7b0`。最终模拟设备写入次数 TCP 1、UDP 1、HTTP 2、Modbus 1、S7 2、OPC UA 0。未提交或推送代码。

`ui-cloud-0909` 和旧 `ui-protocol-0909` 均已停止，避免持续写测试数据；未删除容器、网络、产品、设备或记录。七份流程和组件安装保留，设备模拟器保持运行以保留写入计数。可从内置浏览器启动临时实例继续演示。

## 验收范围之外与保留边界

- OPC UA 控制在默认受管实例中不可用，当前已在界面和服务端明确拒绝开启。待独立受控协议执行节点正式交付后才可验收，不通过开启通用 `exec` 绕过。
- 状态同步基于实时观测及明确的停止/离线事件；不把静默自动判断为离线。特定物理协议的断链检测、可配置无响应超时策略仍需单独交付。容量导致来源不完整时禁止自动 OFFLINE，诊断明确需要人工核对，不声称能自动解除冲突。
- Cloud 服务端拓扑更新归属校验仍需独立加固；本轮只增加 Edge 侧查询和校验，没有改动 Cloud 状态更新实现。
- 正常 Fastify `onClose` 取消/等待路径已测试；现有全进程没有 SIGTERM/SIGINT 收尾装配，本次 Docker 重启验证的是持久化恢复，不代表优雅退出已验收。
- 本轮没有重跑整套 `apps/manager && pnpm verify`；已完成上述定向真实容器、MQTT、Cloud 及页面验证。发布验收和实体仪表/PLC 兼容性仍是独立门槛。

当前通过范围为 TCP、UDP、HTTP 定时、HTTP 推送、Modbus-TCP、OPC UA、S7 七种采集模板及六种已开放控制模板。CoAP、BACnet、IEC104、串口 RTU、OPC UA 控制及实体仪表/PLC 兼容性不在本次通过范围内。
